{
  "name": "qute",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "workspaces": ["packages/*"],
  "engines": {
    "node": ">=23.0.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev || true",
    "build": "pnpm -r --parallel build"
  }
}
